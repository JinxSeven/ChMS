services:
  api:
    image: ${DOCKER_REGISTRY-}api
    container_name: chms_api
    build:
      context: .
      dockerfile: api/Dockerfile
      args:
        APP_UID: 1000

    ports:
      - "8080:8080"
      # Uncomment HTTPS port for Visual Studio .dcproj | Leave it commented for Docker Compose.
      # - "8081:8081"

    depends_on:
      - database
      - otel-collector

  database:
    image: postgres:18.3
    container_name: chms_db

    environment:
      POSTGRES_DB: chms
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: letmein

    volumes:
      - chms_db_data:/var/lib/postgresql

    ports:
      - "5432:5432"

  #LOKI
  loki:
    image: grafana/loki:3.0.0
    container_name: chms_loki
    command: -config.file=/etc/loki/loki-config.yaml
    volumes:
      - ./otel/loki-config.yml:/etc/loki/loki-config.yaml
      - loki_data:/loki
    ports:
      - "3100:3100"

  #TEMPO
  tempo:
    profiles: [OTEL]
    image: grafana/tempo:2.4.1
    container_name: chms_tempo
    command: ["-config.file=/etc/tempo.yaml"]
    volumes:
      - ./otel/tempo-config.yml:/etc/tempo.yaml
      - tempo_data:/tmp/tempo
    ports:
      - "3200:3200"

  #PROMETHEUS
  prometheus:
    profiles: [OTEL]
    image: prom/prometheus
    container_name: chms_prometheus
    volumes:
      - ./otel/prometheus-config.yml:/etc/prometheus/prometheus.yml
      - prometheus_data:/prometheus
    ports:
      - "9090:9090"

  #OTEL COLLECTOR (CORE)
  otel-collector:
    image: otel/opentelemetry-collector-contrib:latest
    container_name: chms_otel
    command: ["--config=/etc/otel/config.yaml"]

    volumes:
      - ./otel/otel-config.yml:/etc/otel/config.yaml

    ports:
      - "4317:4317"
      # - "4318:4318"
      # - "9464:9464"

    depends_on:
      - loki
      # - tempo
      # - prometheus

  grafana:
    image: grafana/grafana:10.4.0
    container_name: chms_grafana

    ports:
      - "3000:3000"

    environment:
      GF_SECURITY_ADMIN_USER: admin
      GF_SECURITY_ADMIN_PASSWORD: admin@123
    
    volumes:
      - grafana_data:/var/lib/grafana
      - ./otel/datasources.yml:/etc/grafana/provisioning/datasources/datasources.yml

    depends_on:
      - loki
      # - tempo
      # - prometheus

volumes:
  chms_db_data:
  loki_data:
  tempo_data:
  prometheus_data:
  grafana_data: